# PLC 气密检测上位机技术实施方案

## 1. 项目概述

### 1.1 建设目标

开发一套运行于 Windows 工控机的气密检测数据采集上位机，实现扫码、工位选择、PLC 数据采集、检测结果匹配、SQLite 持久化、实时展示、作业指导书查看和 CSV 导出。

系统应满足以下核心目标：

- 扫码数据与 PLC 检测结果按工位准确匹配。
- PLC 标志位按上升沿触发，同一检测结果只入库一次。
- 通信、轮询、导出等耗时操作不阻塞界面。
- PLC、扫码枪、数据库及业务参数统一通过 INI 配置。
- 工位和产品可在运行期维护，历史检测记录不受配置变更影响。
- PLC 断线后自动重连，异常过程有界面提示和文件日志。
- 兼容 Windows 7 工控机，并具备现场联调所需的字序、字节序配置能力。

### 1.2 建设范围

本项目包含：

1. WinForm 上位机客户端。
2. Modbus TCP 通信模块。
3. 键盘模拟及串口扫码枪接入模块。
4. SQLite 本地数据库。
5. 工位、产品、作业指导书管理和查看功能。
6. 检测记录展示、详情查看和 CSV 导出。
7. INI 配置、运行日志、自动建库和部署说明。

本项目不包含 PLC 程序开发、PLC 网络施工、气密仪自身通信程序及历史检测记录删除功能。PLC 侧需按约定提供工位号、检测字符串、结果标志和应答处理逻辑。

## 2. 技术路线

### 2.1 推荐技术选型

| 类别 | 选型 | 说明 |
| --- | --- | --- |
| 开发语言 | C# | 工业上位机常用，维护成本低 |
| 运行框架 | .NET 8（WinForm） | 采用 win-x64 自包含发布，目标机无需单独安装运行时 |
| 界面框架 | WinForm + SunnyUI | 主窗体及业务弹窗统一使用 SunnyUI 风格 |
| PLC 通信 | NModbus、Modbus TCP | PLC 作为服务端，上位机作为客户端 |
| 数据库 | Microsoft.Data.Sqlite | 单机部署，无需独立数据库服务 |
| 配置文件 | UTF-8 INI | 参数可现场修改，不依赖注册表 |
| 日志 | NLog | 文件滚动、级别控制和异常堆栈记录 |
| 数据导出 | 自研 CSV 导出 | UTF-8 BOM，兼容中文 Excel |

### 2.2 设计原则

- 分层设计：界面、业务、设备通信、数据访问、配置和模型相互解耦。
- 单一数据入口：检测记录只能由 PLC 上升沿匹配成功后写入数据库。
- 历史快照：检测记录冗余保存工位名称和产品名称，配置变更不改变历史。
- 配置驱动：地址、周期、字序、字节序、字符串格式和应答值均可配置。
- 故障隔离：PLC、扫码枪、图片和导出异常不得导致主程序退出。
- 参数化 SQL：数据库访问全部使用参数化语句，避免 SQL 注入和特殊字符错误。

## 3. 总体架构

系统分为六层：

```text
┌──────────────────────────────────────────────┐
│ UI 层：主窗体、工位配置、扫码、作业指导书、详情 │
├──────────────────────────────────────────────┤
│ 业务层：待检测管理、匹配入库、产品管理、导出    │
├──────────────────────────────────────────────┤
│ 设备层：ModbusService、ScannerService          │
├──────────────────────────────────────────────┤
│ 数据层：SQLite 建库、配置 CRUD、检测记录查询    │
├──────────────────────────────────────────────┤
│ 基础层：INI、日志、CSV、校验、线程封送          │
├──────────────────────────────────────────────┤
│ 外部设备：PLC、扫码枪、气密仪（由 PLC 汇总）    │
└──────────────────────────────────────────────┘
```

### 3.1 建议解决方案结构

```text
AirtightInspection.sln
└─ AirtightInspection.WinForms
   ├─ Config
   │  ├─ AppConfig.cs
   │  └─ IniFile.cs
   ├─ Data
   │  ├─ DatabaseInitializer.cs
   │  ├─ StationRepository.cs
   │  ├─ ProductRepository.cs
   │  └─ ScanRecordRepository.cs
   ├─ Models
   │  ├─ StationConfig.cs
   │  ├─ ProductConfig.cs
   │  ├─ PendingRecord.cs
   │  └─ ScanRecord.cs
   ├─ Services
   │  ├─ ModbusService.cs
   │  ├─ ScannerService.cs
   │  ├─ PendingRecordService.cs
   │  ├─ InspectionService.cs
   │  ├─ ManualService.cs
   │  └─ CsvExportService.cs
   ├─ Utilities
   │  ├─ EncodingHelper.cs
   │  ├─ ValidationHelper.cs
   │  └─ UiThreadHelper.cs
   ├─ Forms
   │  ├─ MainForm.cs
   │  ├─ ScanStationForm.cs
   │  ├─ StationConfigForm.cs
   │  ├─ PasswordForm.cs
   │  ├─ ManualViewerForm.cs
   │  └─ RecordDetailForm.cs
   ├─ Resources
   │  └─ Icon.ico
   ├─ Program.cs
   ├─ Config.ini
   └─ NLog.config
```

运行期目录：

```text
程序目录
├─ AirtightInspection.exe
├─ Config.ini
├─ Icon.ico
├─ Data\mydb.db
├─ Logs\yyyy-MM-dd.log
└─ ProductManual\*.jpg|*.png|*.bmp|*.gif
```

## 4. 核心业务流程

### 4.1 扫码与待检测流程

1. 扫码枪通过键盘模拟或串口提交条码。
2. 系统清理条码首尾空白并验证非空。
3. 系统查询当前已启用工位；无可用工位时终止并提示。
4. 弹出工位选择窗口，显示条码和工位列表。
5. 用户确认后，系统截取当时选中的产品名称。
6. 待检测数据按工位号存入内存字典，不立即写数据库。
7. 若该工位已有待检测记录，用户确认后才能覆盖。
8. 主界面实时刷新待检测列表。
9. `WaitTimeoutSec > 0` 时，后台定时清除超时项并告警。

待检测内存结构建议使用 `ConcurrentDictionary<int, PendingRecord>`，键为工位号。对新增、覆盖、匹配移除和超时清理统一通过 `PendingRecordService` 执行，以避免轮询线程与 UI 线程并发修改。

### 4.2 PLC 采集与入库流程

```text
连接 PLC → 周期读取 Flag 和 StationNo
                     │
                     ├─ Flag 不为 1：继续轮询
                     │
                     └─ 首次读到 Flag=1
                              ↓
                     读取工位号、气密字符串
                              ↓
                     按工位查找待检测记录
                       ┌──────┴──────┐
                     未找到         找到
                       ↓              ↓
                    告警不入库     SQLite 事务入库
                                      ↓
                               成功移除待检测项
                                      ↓
                         写 ACK=2（失败写 ACK=3）
```

处理原则：

- 保存上一轮规范化后的标志值；首次读到标志位为 1 时处理一次，标志持续为 1 时不重复处理。处理成功后写 2，失败后写 3。
- 处理完后即更新边沿状态；标志保持 1 时不得重复入库。
- 数据库插入成功后再从待检测字典移除，避免入库失败导致条码丢失。
- 数据库插入与内存移除之间应由业务服务控制；如 ACK 写入失败，记录仍视为已入库，依靠边沿锁定避免重复。
- 无匹配待检测项时不落库、不写成功 ACK；是否写失败 ACK 建议通过额外配置控制，默认写失败 ACK=3，防止 PLC 长时间等待。
- 主界面更新统一使用 `BeginInvoke`，PLC 轮询不得直接操作控件。

### 4.3 边沿状态初始化

软件刚连接 PLC 时，应先读取一次当前标志作为初始状态：

- 当前为 0：进入正常边沿监听。
- 当前为 1：立即读取工位号和气密数据并尝试匹配待检测记录；同一次持续高电平只处理一次。

PLC 收到上位机写回的 2 或 3 后应将 D4000 清零，再开始下一次检测流程。

## 5. Modbus 通信设计

### 5.1 地址约定

NModbus API 使用从 0 开始的协议地址，不应把文档显示地址 40001 直接传入 API。按当前需求：

| PLC 软元件 | 含义 | 协议起始地址 | 长度 |
| --- | --- | ---: | ---: |
| D4000 | 标志/应答 DINT | 4000 | 2 个寄存器 |
| D4002 | 工位号 DINT | 4002 | 2 个寄存器 |
| D5000 | 气密字符串 | 5000 | 默认 20 个寄存器 |

正式联调前须用 Modbus 测试工具核对 PLC 映射。部分工具以 40001 为显示基准，容易产生偏移 1 的问题。

### 5.2 DINT 解码

默认按“低字在前、每个寄存器低字节在前”组合：

```text
value = (highWord << 16) | lowWord
lowWord  = registers[0]
highWord = registers[1]
```

为了兼容 PLC 实际组态，INI 增加：

```ini
[Register]
WordOrder=LowHigh       ; LowHigh / HighLow
ByteOrder=BigEndian     ; BigEndian / LittleEndian（单寄存器内字节）
```

读取工位号时以现场写入 1、2、3 的测试值校验，标志位以 0、1、2、3 校验。写应答时必须使用与读取相同的字序规则，并一次写入两个保持寄存器。

### 5.3 字符串解码

字符串格式必须由 PLC 与上位机共同确认。建议支持以下配置：

```ini
[Register]
AirtightStrLen=20
CharsPerRegister=2      ; 1 / 2
StringByteOrder=LowHigh ; HighLow / LowHigh
StringEncoding=ASCII    ; ASCII / UTF8 / GBK
StringHeaderBytes=0     ; 0 表示无长度头，2 表示跳过两个长度字节
```

默认按每个寄存器 2 个 ASCII 字符、寄存器低字节在前、无长度头解码。解码后移除 `STX、ETX、CR、LF、NUL` 等控制字符及首尾空格。若气密仪返回中文，应改用 GBK 或双方明确的编码，并保证 PLC 字符串长度足够。

### 5.4 连接与重连

- 使用独立后台任务维护 TCP 连接和轮询循环。
- 每次读写设置超时，异常后关闭失效连接。
- 按 `ReconnectIntervalMs` 自动重连。
- 使用 `CancellationToken` 支持程序安全退出。
- 同一 Modbus 客户端的读写由互斥锁串行化，避免并发访问；通信验证统一使用实际 PLC 的正常轮询与触发流程。
- 状态栏展示“未连接、连接中、已连接、故障”四种状态。
- 连续相同错误做日志节流，避免 PLC 长时间离线导致日志爆量。

## 6. 扫码枪设计

### 6.1 键盘模拟模式

- 仅主窗体激活时接收扫码按键。
- 使用按键缓冲区，以回车作为一次扫码结束。
- 忽略控制键；支持数字、字母及常用可打印字符。
- 可增加最大字符数和扫码间隔阈值，防止人工键盘输入被误识别为扫码。
- 弹窗已打开时暂停主窗体扫码监听，防止重复弹窗。

建议新增配置：

```ini
[Scanner]
MaxBarcodeLength=200
KeyboardCharTimeoutMs=100
```

### 6.2 串口模式

- 按 INI 配置打开串口。
- 后台接收数据，以 `LineEnd` 指定的结束符分帧。
- 串口断开或被占用时告警，并按间隔尝试重新打开。
- 收到完整条码后统一调用与键盘模式相同的扫码业务入口。

## 7. 数据库方案

### 7.1 数据表

按需求建立 `StationConfig`、`ProductConfig` 和 `ScanRecord` 三张表，所有时间统一保存为 `yyyy-MM-dd HH:mm:ss.fff`。

索引建议：

```sql
CREATE INDEX IF NOT EXISTS IX_ScanRecord_RecordTime
ON ScanRecord(RecordTime DESC);

CREATE INDEX IF NOT EXISTS IX_ScanRecord_StationNo
ON ScanRecord(StationNo);
```

### 7.2 数据一致性

- 首次启动自动创建 Data 目录、数据库文件、表和索引。
- 工位号、产品名由数据库唯一约束做最终兜底。
- 新增和修改前先做业务校验，捕获唯一约束异常并友好提示。
- 删除工位或产品只删除配置表数据，不处理 `ScanRecord`。
- 检测记录不提供删除入口。
- 数据库连接采用“短连接、用完释放”；写入使用事务。
- 启动时执行简单可用性检查，数据库损坏或无写权限时阻止采集并明确告警。

### 7.3 查询与展示

主表按 `DetectTime` 或 `Id` 倒序加载。为避免记录长期累积导致启动缓慢，界面建议分页或默认展示最近 1000 条；CSV 导出仍可按“全部记录”查询导出。若业务要求主表一次显示全部数据，应在验收前评估数据量和内存占用。

## 8. 界面方案

### 8.1 主窗体

主窗体采用无系统标题栏设计，包括：

- 自绘标题区：系统名称、最小化、关闭和窗体拖动。
- 状态工具区：PLC 状态、当前工位号、轮询状态、扫码焦点提示。
- 产品区：产品下拉框及新增、重命名、删除、作业指导书按钮。
- 检测记录区：时间、工位号、工位名称、产品名称、条码、气密字符串、状态。
- 待检测区：工位号、条码、产品名、扫码时间和剩余/超时状态。
- 操作区：工位配置、产品配置、作业指导书、导出 CSV；不提供独立测试连接功能。
- 日志卡片：仅显示连接变化、扫码、配置变更、超时、触发、入库和导出等关键事件。

关闭程序时，如仍存在待检测记录，应提示这些内存数据将在退出后丢失，由用户确认是否退出。

### 8.2 权限校验

工位和产品的新增、修改、删除均调用统一密码弹窗。密码按 INI 原始值比较，界面以掩码显示。密码失败只记录操作类型，不在日志中输出用户输入值或正确密码。

当前方案按需求采用 INI 明文密码，能防止普通误操作，但不属于强安全机制。如现场有信息安全要求，可升级为密码哈希或 Windows 用户权限控制。

### 8.3 作业指导书

- 每次打开重新扫描配置目录。
- 文件名按不区分大小写方式包含产品名称即匹配。
- 按文件名稳定排序。
- 单页显示一张，支持上一张、下一张、方向键、50%～300% 缩放和拖动。
- 图片使用临时流或克隆方式加载，及时释放原文件句柄，避免图片无法被替换或删除。
- 损坏图片跳过并记日志，全部不可用时显示无可用图片。

### 8.4 CSV 导出

- 通过保存对话框选择路径。
- 后台逐行写入，编码为带 BOM 的 UTF-8。
- 字段中的逗号、双引号、回车换行按 CSV 规则转义；双引号写成两个双引号，必要时整字段加双引号。
- 导出期间禁用导出按钮并显示进度状态，完成后恢复。
- 使用数据库快照式查询，导出开始后新产生的数据是否包含，以查询时点为准。

## 9. 配置方案

除需求指定配置外，建议补充现场联调参数：

```ini
[App]
Icon=Icon.ico
WaitTimeoutSec=0
DisplayRecordLimit=1000

[Modbus]
IpAddress=192.168.0.10
Port=502
SlaveId=1
TimeoutMs=1000
PollIntervalMs=500
ReconnectIntervalMs=3000
EnableWriteAck=1

[Register]
StationNoAddr=4002
FlagAddr=4000
AirtightStrAddr=5000
AirtightStrLen=20
AckAddr=4000
AckValue=2
AckValueFail=3
WordOrder=LowHigh
ByteOrder=BigEndian
CharsPerRegister=2
StringByteOrder=LowHigh
StringEncoding=ASCII
StringHeaderBytes=0

[Database]
FilePath=Data\mydb.db

[Scanner]
Mode=Keyboard
PortName=COM3
BaudRate=9600
DataBits=8
StopBits=1
Parity=None
LineEnd=13
MaxBarcodeLength=200
KeyboardCharTimeoutMs=100

[Log]
Level=Info
MaxFileSizeMB=10

[Security]
Password=123456

[Manual]
Folder=ProductManual
```

INI 缺失时程序生成默认配置，单项无效时使用默认值并写警告日志。涉及通信、数据库路径和扫码串口的参数建议重启生效；若提供“重新加载配置”，应先安全停止相关服务，再加载并重启，不能在轮询过程中直接替换连接对象。

## 10. 异常处理与日志

| 异常场景 | 处理策略 |
| --- | --- |
| PLC 断线 | 状态变红、记录日志、自动重连，界面继续可操作 |
| PLC 地址或字序错误 | 显示原始寄存器调试信息，联调后修正 INI |
| 标志触发但无待检测项 | 不生成检测记录，界面和文件日志告警，按约定决定是否写失败 ACK |
| 数据库写入失败 | 保留待检测项、写失败 ACK、告警并记录异常堆栈 |
| ACK 写入失败 | 记录通信错误；已入库数据不回滚，边沿状态防止本次重复入库 |
| 扫码枪串口不可用 | 告警并自动重试，不影响 PLC 状态显示和历史查询 |
| 待检测超时 | 从内存移除，界面提示并记关键日志，不写 ScanRecord |
| 图片损坏 | 跳过当前文件并继续加载其他图片 |
| CSV 写入失败 | 删除未完成的临时文件或保留 `.tmp` 供排查，提示失败原因 |
| INI 参数非法 | 使用安全默认值，日志记录键名和无效值 |

日志分两类：

- 界面日志：关键信息，限制最大显示条数，避免长期运行内存增长。
- 文件日志：包含详细异常、通信状态、关键原始值及调用堆栈，按大小滚动并保留一定数量归档。

日志不得记录密码；条码是否完整记录需结合现场数据合规要求确定。

## 11. PLC 接口约定

PLC 侧建议状态机：

1. 检测完成后写入 D5000 起始的气密字符串。
2. 写入 D4002 起始的工位号 DINT。
3. 最后将 D4000 标志 DINT 由 0 写为 1。
4. 上位机读取并入库成功后写 2，失败写 3。
5. PLC 收到 2 或 3 后执行相应处理，并将 D4000 清零。
6. 下一条结果只能在 D4000 已清零后再次将其置 1，形成明确的新上升沿。

PLC 写数据的顺序必须遵循“字符串和工位号先稳定、标志最后置 1”，避免上位机读取到尚未更新完整的数据。若 PLC 无法保证同一扫描周期内数据稳定，可增加数据就绪延时或双读一致性校验。

## 12. 测试与验收方案

### 12.1 单元与模块测试

- INI 缺失、中文、非法数值和默认值测试。
- DINT 四种字序/字节序组合测试。
- 字符串控制字符清理、长度头和单双字符寄存器测试。
- 产品名校验、重名和非法字符测试。
- CSV 逗号、引号、换行和中文测试。
- 待检测新增、拒绝覆盖、确认覆盖、超时和并发移除测试。

### 12.2 集成测试

使用 Modbus TCP 模拟器或 PLC 实机：

1. 设置 D4002 为工位 1，确认上位机显示正确。
2. 在 D5000 写入包含 STX/ETX/CR/LF/NUL 的测试字符串，确认清理结果。
3. 扫码并选择工位 1，确认数据库尚无记录。
4. 将 D4000 从 0 置 1，确认仅生成一条记录并返回 ACK=2。
5. 保持 D4000 为 1 多轮，确认不重复入库。
6. 将 D4000 清零后再次置 1，确认产生新事件。
7. 测试工位不匹配、无待检测项、数据库只读等失败场景。
8. 拔掉网线后确认自动重连且 UI 不冻结。

### 12.3 验收重点

- 未配置 INI 也能使用默认参数启动。
- 工位、产品维护均受密码保护且即时生效。
- 待检测状态只在内存存在，同工位最多一条。
- PLC 0→1 触发只入库一次，历史记录准确可追溯。
- 断线重连和长时间运行期间界面无明显卡顿。
- 作业指导书匹配、翻页、缩放、拖动正常。
- CSV 用 Excel 打开中文无乱码，内容与数据库一致。
- 删除或重命名配置不会修改历史检测记录。

## 13. 实施计划

按单人开发估算，建议周期为 15～20 个工作日，PLC 接口具备且联调条件稳定时可按下表推进：

| 阶段 | 工作内容 | 预计工期 |
| --- | --- | ---: |
| 1. 需求与接口确认 | PLC 地址、字序、字符串、ACK 时序、目标系统确认 | 1～2 天 |
| 2. 工程与基础模块 | 项目框架、INI、日志、SQLite 建库 | 2 天 |
| 3. 配置管理 | 工位、产品、密码校验、数据访问 | 2～3 天 |
| 4. 扫码与待检测 | 键盘/串口扫码、弹窗、覆盖、超时 | 2～3 天 |
| 5. PLC 采集 | 通信、解码、边沿、匹配、入库、ACK、重连 | 3～4 天 |
| 6. 辅助功能 | 作业指导书、详情、CSV 导出、自定义窗体 | 2～3 天 |
| 7. 测试联调 | 模拟器、PLC 实机、异常和稳定性测试 | 2～3 天 |
| 8. 部署交付 | 发布包、配置样例、操作说明、问题整改 | 1～2 天 |

## 14. 交付物

1. Visual Studio 完整解决方案及源码。
2. 可直接运行的 Release 发布包。
3. 带中文注释的 `Config.ini` 默认配置。
4. SQLite 初始化数据库或自动建库脚本逻辑。
5. `Icon.ico` 及程序图标配置。
6. 操作使用说明、部署说明和 PLC 联调说明。
7. 测试记录及验收清单。
8. NuGet 依赖及版本清单。

## 15. 项目风险与待确认项

项目启动前必须确认：

1. 目标工控机操作系统版本和位数；当前交付采用 .NET 8、Windows x64 自包含发布。
2. PLC Modbus 地址是否按 0 基协议地址还是 40001 显示地址提供。
3. DINT 的寄存器字序和单寄存器字节序。
4. 气密字符串的编码、每寄存器字符数、长度头、最大长度和实际样例。
5. PLC 对成功 ACK=2、失败 ACK=3 的处理及清零时序。
6. 标志触发但无匹配条码时，PLC 是否要求收到失败 ACK。
7. 软件启动时 PLC 标志已为 1 的处理策略。
8. 条码允许字符、最大长度以及是否需要唯一性约束。
9. 预计日数据量、数据库保留年限和备份方式。
10. SunnyUI 具体版本及商业使用许可要求。

其中第 2～7 项属于 PLC 接口关键项。未确认前可以按默认值完成开发，但必须在现场联调阶段以实测值修正，不能仅依据软元件名称推断。

## 16. 结论

本方案采用 WinForm 分层架构，以“扫码形成工位待检测项、PLC 上升沿触发匹配、数据库成功后应答”为业务主线。该设计能够满足单机气密检测产线的数据采集、追溯和现场维护要求，并通过边沿锁定、历史冗余、后台通信、自动重连及可配置解码降低重复入库、界面阻塞和 PLC 兼容风险。

建议先完成 PLC 接口表和字符串样例确认，再进入编码阶段；在开发中期使用 Modbus 模拟器完成闭环测试，最后以 PLC 实机验证地址、字序及 ACK 状态机。
